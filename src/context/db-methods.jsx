import React from "react";
import { db } from "../firebase";
import { doc, setDoc } from "firebase/firestore"; 




export function AddInfluencer(currentUser) {
    const influencerRef = doc(db,"influencers",);
  return <div></div>;
}
